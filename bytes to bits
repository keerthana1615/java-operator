import java.util.Scanner;

public class Bytes_to_bits
{
	public static void main(String[] args) {
		Scanner s = new Scanner(System.in);
		int bytes = s.nextInt();
		int bits = bytes*8;
		    System.out.println(bits+" bits");
		
	}
}
